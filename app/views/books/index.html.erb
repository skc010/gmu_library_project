<h1>Books</h1>
<%= page_entries_info @books %>
<ol>
	<% @books.each do |book| %>
      <li>
      	<img class='all_books' src=<%= book.image_cover_url %> alt='cover'/> <%= link_to(book.title, book) %> by 
      	<%= book.author.name %> 

      </li>

	<% end %>
</ol>
<%= paginate @books %>